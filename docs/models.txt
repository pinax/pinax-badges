Models in ``brabeion``
======================

.. module:: brabeion.models

.. class:: BadgeAward(models.Model)

    .. attribute:: user

        The user who was awarded this badge.

    .. attribute:: awarded_at

        The ``datetime`` that this badge was awarded at.

    .. attribute:: slug

        The slug for the ``Badge`` this refers to.

    .. attribute:: name

        The name for the ``Badge`` this refers to, for the approrpiate level.

    .. attribute:: description

        The description for the ``Badge`` this refers to, for the approrpiate
        level.


Custom Model in ``brabeion``
======================

.. code-block:: python
   :emphasize-lines: 3,5

    from brabeion.models import BadgeAward

    class CustomBadgeAwardModel(BadgeAward):
        pass


setting definitions (settings.py)

.. code-block:: python
   :emphasize-lines: 3,5

    BRABEION = {
        'ABSTRACT_MODEL': True,
        'BADGEAWARD_MODEL': 'customapp.models.CustomBadgeAwardModel'
    }
